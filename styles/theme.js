import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Roboto Mono",
    body: "inconsolata",
  },
  colors: {
    mainRed: "#AB0F62",
  },
});

export default theme;
